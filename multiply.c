/** \brief Multiplica valor de 'a' por valor de 'b'
 *
 * \param Toma valor de variable 'a'
 * \param Toma valor de variable 'b'
 * \return Devuelve a*b (valor de 'a' multiplicado valor de 'b')
 *
 */

#include <stdio.h>
#include <stdlib.h>
#include "multiply.h"

int multiply (int a, int b)
{
    return a*b;
}
